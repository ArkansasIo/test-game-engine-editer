// Command System: Actions, shortcuts, toolbar bindings
pub struct CommandInfo {
    pub id: String,
    pub label: String,
    pub tooltip: String,
    pub shortcut: Option<String>,
}

    pub fn register_command(_cmd: CommandInfo) {
    // TODO: Register command
}
