# Assets (optional)

This scaffold is UI-first. If you want the toolbar icons / panels to match Unreal more closely:
- Add PNG/SVG icons here and load them as egui images.
- Add a small atlas + icon registry.

Next step:
- Create `src/ui/icons.rs` with an enum of icon IDs and a loader.
